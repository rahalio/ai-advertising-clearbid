/**
 * ExecuteDefineConversionAction - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: campaigns
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */
import { NotFoundError, ValidationError } from "../errors";
export class ExecuteDefineConversionAction {
    context;
    idGenerator;
    conversionDefinition;
    constructor(context, idGenerator, conversionDefinition) {
        this.context = context;
        this.idGenerator = idGenerator;
        this.conversionDefinition = conversionDefinition;
    }
    async execute(input) {
        // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
        const correlationId = this.idGenerator.cmpId();
        // All field names extracted from OpenAPI spec: actionType, attributionWindowHours, id, systemOfRecord, valuePerAction
        // Validate input
        if (!input) {
            throw new ValidationError("Input is required");
        }
        if (!input.campaignId) {
            throw new ValidationError("campaignId is required");
        }
        const campaignId = input.campaignId;
        // Load entity via repository (pass correlation ID for response meta)
        const entity = await this.conversionDefinition.defineConversionAction({ campaignId: campaignId, orgId: this.context.getOrgId(), correlationId });
        if (!entity) {
            throw new NotFoundError(`Entity not found: ${campaignId}`);
        }
        return entity;
    }
}
